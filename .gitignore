# Delivery artifacts (generated by package-for-delivery.ps1)
deploy/images.tar
*.zip

# Environment files — commit only .env.example, never .env with real secrets
.env
deploy/.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db
