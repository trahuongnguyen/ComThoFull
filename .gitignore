# Delivery artifacts (generated by package-for-delivery.ps1)
deploy/images.tar
*.zip

# Environment overrides (keep .env.example committed, never .env with secrets)
.env.local
.env.production

# OS
.DS_Store
Thumbs.db
