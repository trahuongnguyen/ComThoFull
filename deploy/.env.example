# Copy file này thành .env và điền giá trị thực trước khi deploy.
# cp .env.example .env

# Mật khẩu cơ sở dữ liệu
DB_PASSWORD=your-strong-password-here

# Khóa bảo mật JWT — dùng chuỗi ngẫu nhiên ít nhất 32 ký tự
JWT_SECRET=your-random-jwt-secret-at-least-32-chars
